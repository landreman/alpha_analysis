* If you need to understand the workings of the booz_xform package, look at its
  github repository at https://github.com/hiddenSymmetries/booz_xform
* Don't create any new virtual environment. Use the conda environment
  20250627-01-libE
* When generating new code, prefer simple and short. For example, when making a
  new standalone script, don't add capability for command-line arguments unless
  asked to.